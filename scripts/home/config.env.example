# 复制为 config.env 并填写（只需做一次）
# cp config.env.example config.env

# ROS 发行版
ROS_DISTRO=humble

# 机械臂 stream API 端口
STREAM_API_PORT=8001

# LeRobot conda 环境名（没有可留空，跳过 lerobot 命令）
LEROBOT_CONDA_ENV=lerobot

# 可选：Tailscale 主机名（仅用于 status 显示）
TAILSCALE_HOST=dummy-home
